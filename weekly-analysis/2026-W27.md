# L1 每周分析 | 2026-07-05 (W27)

> 分析区间：2026-06-29 ~ 2026-07-05（7天）
> AI 视角：第一人称（小龙虾）

---

## 本周模式识别

### 重复出现的主题

1. **飞书通道稳定性**：本周经历了一次重大飞书入站断线（~18h，dev_20260701_001），暴露了"修一个 bug 留一个 bug"的典型模式——6/29 误关 openclaw-lark 插件导致入站中断，但当时只验证了 outbound 没验证 inbound。修复后连续 22 次 5h reset 验证，飞书通道进入业务态。

2. **水卡数据完整性**：water_tracker.py 本周连续暴露 3 个潜伏 bug（6/29「了」字误判 override、6/29 A 分支「第X杯水」正则不兼容、6/30 append 模式硬编码 +1），根因都是"baseline 模式上线时测试覆盖不足"——只测了最简单的单杯场景，没覆盖多杯/补录/语义歧义。

3. **Token Plan 节奏规律**：本周确认了 5h reset 节奏（连续 22 次验证，80% 是 5h 整点，20% 是 4h），同时确认"扣减速率不可线性外推"——凌晨衰减是真实的，但具体 reset 时点由 MiniMax 服务端控制，不可靠预测替代实时检查。

4. **向量库数据断层**：6/29 发现 vector_store.py 只索引 corrections.md + deviations/ scenes，6/1 之后的 L0/MEMORY/log 全部未入库（潜伏 28 天）。修复后新增 extract_chunks_from_l0() 函数，覆盖 L0 + MEMORY.md + daily-working-log，但 corrections extract 函数未同步格式迁移（dev_20260629_001）。

5. **反信息茧房高频触发**：本周多次出现"我以为/我推断/我预测"型错误（Token reset 预测错误 2 次、飞书通道修复误判 4 轮、openclaw-lark 插件遗漏），共同根因是"经验型推断"替代了"实测验证"。

### 规则触发频率

- **「三方核验」原则**：本周触发 4 次（Token reset 预测、飞书通道真链路验证、向量库数据断层定位、water_tracker bug 定位），每次都是"我以为"先跑、Lee 纠偏或实测打脸在后。
- **「SDK 上报错误 ≠ 真实原因」**：6/29 water_push 400 错误，SDK 包装成 ECONNRESET，真相是 FEISHU_APP_ID+SECRET 缺失导致 token 过期 23h+。
- **「双向验证」原则**：plugin toggle 类改动必须 out+in 双向验证，本周 openclaw-lark 插件事件是该原则的教科书案例。
- **「selftest 覆盖度」原则**：任何 append 类脚本的 selftest 必须包含 +N 杯多杯场景，本周 water_tracker 的 3 个 bug 全部是覆盖度不足导致。
- **「先查证据再开干」**：7/1 Lee 说"升级到 2026.6.10"，我默认 Lee 没升过，实际 Lee 21 分钟前已完成。

### 新规范 / 新决策

**新立规则：**
- **全中文对话红线**（6/17 升级到 SOUL 永久区）：24h 内第二次触犯 = L0a 反复违反，立即记 corrections + 升级 SOUL。
- **plugin toggle 双向验证**：任何 plugin enable/disable 改动后必须验证 outbound + inbound，不能只验证一个方向。
- **SDK 上报错误 ≠ 真实原因**：feishu SDK 的 4xx/ECONNRESET 报错，第一时间 curl 复现拿真实 code，不信 SDK 上报层包装。
- **/tmp 脚本复用前必须看主体逻辑**：临时脚本不是通用 helper，月报专用脚本误用到关联分析推送 = corrections 重大事故（dev_20260701_001）。
- **cron delivery mode 统一改为 none**：isolated session 里 announce 通道无效（连续 8 次报错），推回执完全交给 prompt 内的 curl 直推。
- **水卡数据 ABC 兜底范式**：A 分支 dry-run + B 分支推送 + C 分支 data 写入，三件套缺一不可，绕过任一环节都是数据丢失。

**新里程碑 anchor：**
- 06-29：向量库数据断层修复（28 天盲区暴露）
- 06-29：水卡 v2 push worker（curl 直连飞书 API，绕过 SDK token 问题）
- 06-30：water_tracker 多杯 bug 修复（潜伏 15 天）
- 07-01：飞书入站断线修复（18h 静默期）
- 07-05：W27 周报落地（cron 大盘 90% 健康率）

---

## 下周关注点

1. **water_tracker 脚本稳定性验证**：本周修了 3 个 bug（A 正则、B「了」字、C append +1），下周需要跑一次完整的 selftest（覆盖 20+ 用例），确认多杯/补录/语义歧义场景全覆盖。

2. **向量库 corrections extract 函数格式兼容性**：6/29 修复 vector_store.py 时发现 corrections 新格式（带【新】标记）和 extract 函数不兼容，导致新格式的 corrections 从未入过库。下周需要验证 extract 函数对新旧两种格式的兼容性。

3. **cron 大盘健康率维持**：本周 cron 健康率 90%（59 / 6 error / 0 disabled），6 个 error 都是历史累积。下周需确认 error cron 是否自愈，或需要 Lee 介入修复。

4. **联合记忆 cron 稳定性**：6/16 落地的联合记忆拉取+学习 cron（id b36e3759，每日 9:15 CST），连续 3 周跑通但联合仓数据始终为 0（shared/daily/ 不存在）。可能 PC 端（CC）未部署或路径不匹配，需要 Lee 确认。

5. **Obsidian 路径同步问题**：W27 周报生成时发现 TOOLS.md 路径写错（写成了 wiki/_archive/2026-05-para-backup/ 而非正确的收藏目录），导致 git pull 验证失败。需修复 TOOLS.md 并检查其他路径准确性。

---

## 附录：本周 dev_ 记录索引

| dev_id | 日期 | 描述 |
|--------|------|------|
| dev_20260629_001 | 06-29 | 向量库数据断层（28 天盲区） |
| dev_20260629_002 | 06-29 | water_push 400 根因（FEISHU token 过期） |
| dev_20260630_001 | 06-30 | 夜读 cron 首跑参考 bookId 章节号错误 |
| dev_20260630_003 | 06-30 | water_tracker append +1 bug（潜伏 15 天） |
| dev_20260630_004 | 06-30 | association deepLink 书名/书号不匹配 |
| dev_20260630_005 | 06-30 | message 工具多 channel 必填 channel=feishu |
| dev_20260701_001 | 07-01 | 飞书入站断线（openclaw-lark 插件误关） |
| dev_20260701_001 | 07-01 | /tmp 脚本误用（corrections 重大事故） |

---

_由小龙虾自动生成 | 2026-07-05 22:00 CST | W27 每周分析_
