# Corrections Log

| Date | What I Got Wrong | Correct Answer | Status |
|------|-----------------|----------------|--------|

**2026-04-16 10:04 | Lee**
> "有其他需要改进时，请及时提醒我。"

→ 行为准则：主动识别并推送系统层面的改进机会，不等用户来问

**2026-04-16 14:11 | Lee**
> "以后录入的待办事项，需要我确认完成才能结束。"

→ 规则：所有新建的待办事项（cron/agentTurn 触发的任务提醒），完成后必须由 Lee 确认，不能自动标记结束
→ 违反此规则的行为：自动标记完成/自动结束任务

**2026-04-16 15:00 | Lee**
> "把主群的日报模板弄成跟进化群日报模板一样了"

→ 根因：创建 cron 时用了相同/相似的 agentTurn message 结构
→ 规则：不同群不同用途，模板必须严格区分
  - 主群日报 = 工作事项总结（今日完成/明日计划/阻塞）
  - 进化群日报 = AI协作反思（AI心得/改进方向）
→ 记录教训：跨群同类任务（都叫"日报"）最容易混，后续创建前先对比现有模板

**2026-04-16 15:02 | Lee**
> "定时的内容，每日请准时发送，不要再有类似重复和不定时的推送。如果推送没成功，请联系我确认。"

→ 规则：
  1. 定时任务必须准时，不重复推送
  2. 推送失败时必须主动联系 Lee 确认（不静默失败）
  3. 每次新建 cron 必须检查是否与现有任务重复或冲突

**2026-04-16 15:23 | Lee**
> "以后如果我添加的事项有冲突，请及时提醒我修改。"

→ 规则：新建 cron 时必须先检查时间冲突（同一时间点多个飞书推送任务）
→ 发现冲突：立即提醒 Lee，建议错开分钟数
→ 记录教训：同时间推送多个 cron 会触发 MiniMax 并发限速，必须物理隔离

**2026-04-16 15:47 | Lee**
> 新闻简报脚本超时优化

→ 教训：多源串行curl（每源12秒）= 最多48秒等待，极易超时
→ 解法：并行curl（max-time 8秒快速失败）+ 临时文件传数据
→ 效果：3分钟超时 → 2.75秒完成，保留所有4个新闻源
→ 记录：bash变量传JSON（含特殊字符）会破坏数据，改用临时文件

## 内容收集必须一次做完整（2026-04-20）
**触发：** Lee 提醒"不要让我再提醒你"

**规则更新：**
收到任何群消息（文章/想法/链接），收集时必须同步完成：
1. 打标签（tags.md）
2. 双向关联（index.md + 关联分析文档）
3. 同步到 Obsidian
4. git 提交

**绝对不能再：**
- 先简单回复"收到已收录"
- 等 Lee 问"做好关联了吗"才补做

**原因：** 一次做完整 vs 两次做，效率差3倍，且容易漏掉关联思路

## 模型使用规则（2026-04-22）
- 定时cron任务默认使用 MiniMax M2.7（不再指定 deepseek/deepseek-chat）
- 如遇 MiniMax 并发错误 → 通知 Lee，考虑切回 DeepSeek
- DeepSeek API Key（****4119）已失效，如需恢复需重新配置
