# L1 主题索引 - 2026-06-07（主题聚类）🦞

> 这是 L0（memory/2026-06-07.md）的**主题聚类版本**，便于跨日检索。
> 周报（evolution-report-2026-06-07.md）已自动生成（19:15），本文件是**手动主题聚类**补充。

## 主题 1：沉默失败（SILENT FAILURE）⚠️ 今日最重磅

**关联 dev**：
- `dev_20260607_003`：安全群 chat_id 错版 2 周（5/21→6/07）— MEMORY.md 写 33 字符，飞书 API 真值 35 字符
- `dev_20260607_005`：6 个 chat_id 全链路验证（18:47-18:48）— `lastDeliveryStatus: "delivered"` 真送达
- `dev_20260607_006`：推送类 cron 全链路 force run 验证（19:39-19:42）— 2 个隐藏错版 + 2 个 `last` 冲突
- `dev_20260607_007`：heartbeat prompt 漏修 chat_id + cron chat_id 审计 cron 建立（20:02-20:07）

**核心教训**：
- **沉默失败 = 没有定期端到端验证的习惯**
- **chat_id 验证要分三层**：① MEMORY.md ↔ 飞书 API ② cron 内部 ↔ MEMORY.md ③ cron prompt 文字 ↔ MEMORY.md
- **prompt 文字里的错版不影响推送，但会误导未来 Agent**
- **修复后必须全量 grep 验证**（`grep -oE "oc_1f77586[a-f0-9]+" | sort -u`）

**永久机制**（已建）：
- `scripts/cron_chat_id_audit.sh`（每天 23:55 自动跑，异常推安全群）
- cron `cron-chat-id-audit-daily`（98620a74，after=2，cooldown=1h）
- 6/6 飞书群全部 35 字符真值，29/29 cron 字符级一致

---

## 主题 2：时区铁律（TIMEZONE DISCIPLINE）⚠️ 系统级红线

**关联 dev**：
- `dev_20260607_002`：时区铁律全量清理（18:28-18:30）— 6 处 naive datetime 全清
- `dev_20260607_004`：时区铁律守护接入 L0 watchdog（18:32-18:37）

**清理的 6 处**：
1. `memory_auto_refine.py:124` — print 日志
2. `memory_importance_scorer.py:168` — print 日志
3. `event_logger.py:35` — 时间戳
4. `daily-briefing.sh:88` — heredoc 内 cutoff
5. `daily-briefing.sh:558` — heredoc 内 cutoff
6. `intuition_snapshot.py:28` — CST 重复定义

**永久机制**（已建）：
- `scripts/datetime_naive_scanner.sh`（纯 shell + grep）
- 接入 l0_watchdog.sh（L0 正常路径顺手扫，不阻断）
- 24h 去重（`/tmp/datetime_naive_scanner.lock`）
- 违规推安全群（35 字符真值）

**验证**：残留扫描 0 处，4 个 .py + 1 个 .sh 语法 OK，intuition_snapshot 跑真实业务 7 条置信度记录正常

---

## 主题 3：L0 写入机制（L0 WRITE MECHANISM）⚠️ 根因修复

**关联 dev**：
- `dev_20260607_001`：L0 实时写入规则连续违反（17:00-17:10）— 漏答 + 二次违规

**根因**：把"L0 实时写入"红线降级成"想起来就写"，**没有自动化机制**

**永久机制**（已建）：
1. `scripts/l0_watchdog.sh`（1.1KB，纯 shell，免疫 timeout）
2. `HEARTBEAT.md` 加「L0 当日记录存在性检查」步骤
3. cron `heartbeat-maintenance`（b9f146c4，每小时）
4. AGENTS.md 加「任务收尾检查」清单（3 项必走）

**端到端验证**：删 L0 → 触发推送 → message ID om_x100b6d60d2eb24a8c181a5948886cb8（成功）

---

## 跨主题模式（ROOT CAUSE PATTERN）

**Lee 催一次 = 8 个隐藏问题**（6/7 实际数）：
- 沉默失败类：4 个（chat_id 错版 + 4 次提醒没人修对）
- 时区铁律类：6 个（5/26 立规则时没顺手全扫）
- L0 机制类：1 个（5/22 立规则时没建守护）

**共同根因**：
- 缺乏"端到端测试"文化
- 缺乏"全量扫描"习惯
- 缺乏"沉默失败"检测

**修复方向（已落地 + 自动化）**：
- ✅ 3 个守护 cron（heartbeat / l0_watchdog / cron_chat_id_audit）
- ✅ 3 个扫描脚本（datetime_naive_scanner / l0_watchdog / cron_chat_id_audit）
- ⏳ 待 B：L0 → L1 → MEMORY.md 自动化流水线（下一步）

---

## L1 候选指标（≥85% 可提升到 MEMORY.md）

| 指标 | 当前置信度 | 来源 |
|------|----------|------|
| "沉默失败" 是系统性模式而非偶发 | 90% | 5/21 5/29 6/2 6/7 4 次提醒均未修对 |
| "chat_id 验证分三层" 是普适原则 | 85% | 3 个 cron 都涉及 → 可推广到任何外部 ID |
| "Lee 一句话挖出 N 个问题" 是当前模式 | 95% | 6/7 实证 1 次 = 8 个问题 |
| "红线条目立规则时要顺手全量扫" | 85% | 5/26 时区、5/22 L0 都没扫 |

---

_生成时间：2026-06-07 20:14 | by 小龙虾（C→A 步骤，A 完成，B 待做）_
