
## 系统 Cron 状态（早间）

- cron-retry-monitor：发现 1 个失败任务（openclaw-config-daily-backup），重试后恢复 running ✅
- 对话-Ontology管道：正常运行 ✅
- 自动化Cron：正常运作（今日任务数20）

## 书籍推荐系统重构（10:00-10:19）

### 完成项
- 晨报（7:00）移除书籍推荐栏目 ✅
- 每日书籍推荐（9:30）改 Pillow 文字排版，不用 AI 生图 ✅
- 书卡排版规范终版：纯白背景 / 书名紫色 / 章节标题蓝色 / 正文深灰 / ★评分
- 书名左对齐偏移 -8px（衬线字体字距修正）
- Emoji 对齐：baseline ≈ y + font_size*0.86，emoji 中心 = emoji_h*0.49
- 评分 ⭐ → ★（CJK 字体兼容）
- cron 已更新：`71fae965`
- MEMORY.md 已更新终版规范

### 新授权
- Lee 授权：确认的决策/规范，无需等待，主动写入 MEMORY.md

## 记忆系统问题（10:23）

### 根本问题
跨 session 记忆丢失：昨天确认的事项，今天新的 session 读不到可执行的细节，重新踩坑。

### 教训提炼
- 评分 ★ 而非 ⭐（CJK字体支持）
- 书名左对齐偏移 -8px（衬线字字距）
- Emoji baseline 对齐公式
- Pillow 图片排版替代 AI 生图
→ 以上均已补入 MEMORY.md「关键教训」章节

### 改进措施
- 确认即写入，不等提醒（已建立触发规则）
- 提炼时去重比对 MEMORY.md（已建立规则）

## Ontology 使用规范（v1，2026-04-28）

### 当前状态
- Graph: `/root/.openclaw/workspace/memory/ontology/graph.jsonl`（68实体/206关系）
- 现有内容：cron 执行记录为主，设计决策实体几乎为空白 ❌
- 缺失：书籍推荐设计规范、cron→规范关联关系

### 使用规范（强制）
**处理 cron 前必须查询 Ontology**，命令：
```bash
python3 /root/.openclaw/workspace/skills/ontology/scripts/ontology_query.py \
  --graph /root/.openclaw/workspace/memory/ontology/graph.jsonl \
  --query "<相关关键词>"
```

**场景：**
1. 修改 cron prompt/payload → 先查：这个 cron 关联哪些设计规范？
2. 更改系统配置 → 先查：有没有矛盾的设计决策？
3. Lee 确认新规范 → 立即手动写入 Ontology（`--relate` 或 `--add`）

### 待补充（本周内）
- 每日书籍推荐 → Pillow 渲染（非 AI 生图）规范实体
- 晨报（7:00）→ 移除书籍推荐
- 推送分类：内容收集群 / 日常安排群 / 新闻群

## Obsidian GitHub 备份配置完成（14:30）

### 完成项
- SSH Key 生成 ✅（~/.ssh/id_ed25519.pub）
- GitHub 仓库连接：git@github.com:leeiceice/xiaolongxia-openclaw.git ✅
- sync_memory_to_obsidian.py 增加自动 git commit+push ✅
- 测试推送成功 ✅

### 协同架构
- 服务器记忆 → ~/Obsidian/ → GitHub（每日23:45，sync_memory_to_obsidian.py驱动）
- 本地Obsidian笔记 → GitHub（每30分钟，Obsidian Git插件驱动，需Lee在PC端安装配置）

## Lee 确认沟通架构（18:04）

### 通信架构（终版）
- **OpenClaw UI Control + LightClaw** → 纯对话，触发所有任务
- **飞书群** → 接收推送 + Lee 发送内容（想法/文字/文章）
- 飞书群消息视为输入，强制进入收集工作流（关联分析 + PARA + Obsidian）

### 飞书群消息处理规则
- Lee 发送任何内容到飞书群 → 我识别类型 → 执行收藏/关联/Obsidian同步
- 想法/文字 → ~/Obsidian/收藏/想法/
- 文章链接 → 完整收藏流程 → ~/Obsidian/收藏/文章/ 或公众号/

## 晚间 Cron 状态（22:03）

- 对话-Ontology管道：正常运行，0实体/0关系（已处理，无新增）✅
